New_Smart_Contract.ipynb
Assesing_Vulnerability.pdf

# ---------------------------------------------------------------- data
# Raw corpora are cloned/derived, never authored here. They are large and
# belong to their original authors; scripts/01_fetch_data.py reconstructs them.
data/raw/
data/derived/

# Generated analysis artefacts. Regenerated by scripts/02-07.
# Tracked exception: the small result tables that back the paper's claims.
results/figures/
!results/tables/
!results/tables/*.csv

# ---------------------------------------------------------------- python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Environments. The environment is reproduced from requirements.txt, not
# committed -- see run.sh for the pinned interpreter.
.venv/
venv/
env/
.conda/

# ---------------------------------------------------------------- tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# ---------------------------------------------------------------- editors/OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ---------------------------------------------------------------- secrets
# The LLM detector reads its key from the environment; never commit one.
.env
*.key
